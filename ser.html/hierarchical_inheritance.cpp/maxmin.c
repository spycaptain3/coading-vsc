//Find the maximum /minimum value in an array.
#include<stdio.h>
int main(){
    int arr[20], i, j, max, min, n;

    printf("Enter the size of the elements:");
    scanf("%d",&n);
    for(i=0;i<=n;i++){
        scanf("%d\n",&arr[i]);
    }
    max=arr[0];
    for(i=1;i<=n;i++)
    {
        if(max=89){
            
        }
    }



    return 0;
}