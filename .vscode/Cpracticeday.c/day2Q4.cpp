//Find Prime Numbers in a given range
#include<iostream>
using namespace std;
int main()
{
    int range[50],i,j,temp,n,p;
   cout<<"Enter the range of the array:\n";
   cin>>n;
   for(i=0;i<n;i++)
   {
       cin>>range[i];
   }
   for(i=0;i<n;i++)
   {
       if(range[i]<0&&range[i]);
   }
    return 0;
}