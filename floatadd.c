#include <stdio.h>
int main(){
    float a,b,s;
    printf("Enter  the two float number: ");
    scanf("%f %f",&a,&b);
    s=a+b;
    printf("sum of the above float value is :");
    printf("%f",s);
    return (0);
}